Generating the docs
----------

Use [mkdocs](http://www.mkdocs.org/) structure to update the documentation. 

Move from the base directory to the docs directory:

    cd docs

Build locally with:

    mkdocs build

Serve locally with:

    mkdocs serve
